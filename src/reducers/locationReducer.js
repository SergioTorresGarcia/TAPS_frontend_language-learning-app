const locationReducer = (state = initialState, action) => {
    switch (action.type) {
        // Handle actions to update location state here
        // For example, you might have an action type like 'UPDATE_LOCATION'
        // and update the relevant properties of the location state
        default:
            return state;
    }
};

export default locationReducer;